// src/app/page.tsx

export default function HomePage() {
  // Esta página não precisa de retornar nenhum conteúdo,
  // pois o objetivo é exibir apenas a Navbar que já está no layout.
  return null;
}